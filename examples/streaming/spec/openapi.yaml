openapi: 3.0.3

info:
  title: Connexion Streaming Demo
  version: 1.0.0

servers:
  - url: http://localhost:8080

paths:
  /stream:
    get:
      summary: Stream timer events
      operationId: streaming.stream_handler
      responses:
        '200':
          description: Stream of server-sent events
          content:
            text/event-stream:
              schema:
                type: string
