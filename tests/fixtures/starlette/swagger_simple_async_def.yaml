swagger: "2.0"

info:
  title: "{{title}}"
  version: "1.0"

basePath: /v1.0

paths:
  /starlette_validate_responses:
    get:
      summary: Return a bytes response
      description: Test returning a bytes response
      operationId: fakeapi.starlette_handlers_async_def.starlette_validate_responses
      produces:
        - application/json
      responses:
        200:
          description: json response
          schema:
            type: object
