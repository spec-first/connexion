openapi: 3.0.0
info:
  title: '{{title}}'
  version: '1.0'
paths:
  /welcome:
    get:
      operationId: fakeapi.module_with_exception.something
      responses:
        '200':
          description: greeting response
          content:
            '*/*':
              schema:
                type: object
servers:
  - url: /v1.1
